config:
  # Backtrack is where the root of the project is, relative to this file
  backtrack: .
  # Use this for prefix when building
  prefix: openagency-php-local
images:
  openagency-php:
    type: prod
    context: .
